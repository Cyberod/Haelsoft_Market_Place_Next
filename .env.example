# Base URL for the Django API. Used by server components (src/lib/api.js) and,
# once client islands land in Phase 3+, by the browser too — hence NEXT_PUBLIC_.
# Not a secret: the same URL is already visible in the deployed SPA bundle.
NEXT_PUBLIC_API_BASE_URL=https://haelsoft-marketplace-api.onrender.com/api/v1
